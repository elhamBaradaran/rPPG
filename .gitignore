# Editor / tooling folders
.vscode/
.idea/

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# Model weights and large artifacts
*.pth
*.npy

# Generated run outputs (scripts regenerate these; no need to version them)
pulse_waveform.png
waveform_*.png
windows_*.csv
faces_*.npy

# Raw webcam recordings - hundreds of MB each, and re-recordable.
# The derived results (JSON, PNG) are versioned; the raw frames are not.
motion_*.npz
